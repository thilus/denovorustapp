/*Import the structs and functions from the original source file

#[cfg(test)]
mod tests {

    use denovorust_app::graph::{Graph};
    use denovorust_app::spectrum::{Spectrum};

    #[test]
    fn test_generate_graph_from_spectrum() {
        // Create a sample Spectrum object for testing
        let spectrum = Spectrum::new(/* Initialize with necessary parameters */);
        // Call the generate_graph_from_spectrum method
        let graph = Graph::generate_graph_from_spectrum(&spectrum);


        assert_eq!(graph.all_nodes.len(), /* expected length */);
        assert_eq!(graph.filtered_nodes.len(), /* expected length */);
        
    }
}*/